测试代码在test.m里面，具体的测试过程是：
估算i1到i2的仿射变换矩阵m，然后用m对i1进行仿射变换得到i3，abs(i2-i3)是误差图像。
matlab文件夹中是radon变换程序的结果，里面有2个文件夹zoom和pan，一个是相机缩放情况，另一个是相机平移情况。
opencv文件夹是opencv中自带光流方法的结果，和matlab文件夹对应。输入图像是一致的。
从目前的情况看来，opencv结果好像稍好些，radon变换代码的相关参数应当如何调整？

我目前做的是相机移动情况下的视频背景剔除，希望通过仿射变换参数估计获得视频中相邻帧图像的变换矩阵，做运动补偿。希望能用你的方法做仿射变换参数估计这一步。